name: Build & Release WebSSH

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. 拉代码（需要完整历史和 tags 才能生成 changelog）
      - name: Checkout
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          fetch-tags: true

      # 2. 生成版本号（UTC+8）
      - name: Generate version
        id: version
        run: |
          VERSION=$(TZ=Asia/Shanghai date +%Y%m%d_%H%M%S)
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "当前版本: $VERSION"

      # 2.5 生成更新日志（基于上次 release tag 到当前 HEAD 的提交）
      - name: Generate changelog
        run: |
          # 此时本次的 tag 还没创建，所以最新已有 tag 就是上一个版本
          PREV_TAG=$(git tag --list '20[0-9][0-9][0-9][0-9][0-9][0-9]_[0-9][0-9][0-9][0-9][0-9][0-9]' | sort -r | head -n 1)
          BUILD_TIME=$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')
          {
            echo "## ${VERSION}"
            echo ""
            echo "构建时间: ${BUILD_TIME}"
            echo ""
            if [ -n "$PREV_TAG" ] && [ "$(git rev-parse "$PREV_TAG" 2>/dev/null || echo NA)" != "$(git rev-parse HEAD)" ]; then
              echo "### 与上一版本（${PREV_TAG}）相比"
              echo ""
              git log --no-merges --pretty=format:"- %s (%h) <%an>" "${PREV_TAG}..HEAD"
              echo ""
            else
              echo "### 本次提交"
              echo ""
              git log -1 --pretty=format:"- %s (%h) <%an>%n%n%b" HEAD
              echo ""
            fi
          } > changelog.txt
          echo "--- changelog.txt ---"
          cat changelog.txt

      # 3. Node 环境
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: npm
          cache-dependency-path: webssh/package-lock.json

      # 4. 构建前端
      - name: Build webssh
        run: |
          cd webssh
          npm install
          npm run buildOnly

      # 5. 拷贝 dist → webroot
      - name: Prepare webroot
        run: |
          rm -rf gossh/webroot/*
          mkdir -p gossh/webroot
          cp -r webssh/dist/* gossh/webroot/

      # 6. Go 环境
      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.24.x'
          cache-dependency-path: gossh/go.mod

      # 6.5 CGO ARM64 musl 交叉编译工具链
      - name: Install OpenWrt ARM64 musl toolchain
        run: |
          curl -L https://downloads.openwrt.org/releases/22.03.5/targets/armvirt/64/openwrt-toolchain-22.03.5-armvirt-64_gcc-11.2.0_musl.Linux-x86_64.tar.xz -o /tmp/openwrt-toolchain.tar.xz
          sudo tar -C /opt -xJf /tmp/openwrt-toolchain.tar.xz
          echo "/opt/openwrt-toolchain-22.03.5-armvirt-64_gcc-11.2.0_musl.Linux-x86_64/toolchain-aarch64_cortex-a53_gcc-11.2.0_musl/bin" >> $GITHUB_PATH
          /opt/openwrt-toolchain-22.03.5-armvirt-64_gcc-11.2.0_musl.Linux-x86_64/toolchain-aarch64_cortex-a53_gcc-11.2.0_musl/bin/aarch64-openwrt-linux-musl-gcc --version

      # 7. 构建 Go（二进制 + 内嵌版本）
      - name: Build Go binary
        run: |
          cd gossh

          export CGO_ENABLED=1
          export GOOS=linux
          export GOARCH=arm64
          export CC=aarch64-openwrt-linux-musl-gcc
          export STAGING_DIR=/opt/openwrt-toolchain-22.03.5-armvirt-64_gcc-11.2.0_musl.Linux-x86_64

          go env -w GOPROXY=https://goproxy.cn,direct
          go mod tidy

          go build -trimpath -ldflags="-s -w -linkmode external -extldflags '-Wl,-dynamic-linker,/lib/ld-musl-aarch64.so.1' -X main.version=${VERSION}" -o webssh_${VERSION}
          file webssh_${VERSION}
          readelf -l webssh_${VERSION} | grep 'interpreter' || true

      # 8. UPX 压缩（可选）
      # 直接使用官方静态包，避免 apt 软件源异常导致整个发布流程长时间阻塞。
      - name: Install UPX
        timeout-minutes: 2
        continue-on-error: true
        env:
          UPX_VERSION: 5.2.0
          UPX_SHA256: 3db5d3294707439db97866feab8d75d800f028f48481a40547411824da4288a1
        run: |
          UPX_ARCHIVE="upx-${UPX_VERSION}-amd64_linux.tar.xz"
          curl --fail --location \
            --retry 3 --retry-delay 2 --connect-timeout 10 --max-time 60 \
            "https://github.com/upx/upx/releases/download/v${UPX_VERSION}/${UPX_ARCHIVE}" \
            --output "/tmp/${UPX_ARCHIVE}"
          echo "${UPX_SHA256}  /tmp/${UPX_ARCHIVE}" | sha256sum --check --strict
          tar -xJf "/tmp/${UPX_ARCHIVE}" -C /tmp
          mkdir -p "${HOME}/.local/bin"
          install -m 0755 "/tmp/upx-${UPX_VERSION}-amd64_linux/upx" "${HOME}/.local/bin/upx"
          echo "${HOME}/.local/bin" >> "${GITHUB_PATH}"

      - name: Compress binary
        timeout-minutes: 3
        run: |
          cd gossh
          if command -v upx >/dev/null 2>&1; then
            upx --best --lzma "webssh_${VERSION}" || echo "UPX 压缩失败，继续发布未压缩文件"
          else
            echo "UPX 不可用，跳过压缩并继续发布"
          fi
      
      # 9. 生成 version.txt
      - name: Generate version file
        run: |
          echo "${VERSION}" > version.txt

      # 10. 推送 version.txt + changelog.txt 到独立 version 分支
      - name: Push version.txt & changelog.txt to version branch
        run: |
          mkdir -p /tmp/version-branch
          cp version.txt /tmp/version-branch/version.txt
          cp changelog.txt /tmp/version-branch/changelog.txt

          cd /tmp/version-branch

          git init
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git checkout -b version
          git add version.txt changelog.txt
          git commit -m "update version & changelog to ${VERSION}"

          git remote add origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git push -f origin version

      # 11. 创建 Release（直接上传原始文件，并把 changelog 作为 release body）
      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: ${{ steps.version.outputs.version }}
          body_path: changelog.txt
          draft: false
          prerelease: false
          files: |
            gossh/webssh_${{ steps.version.outputs.version }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
